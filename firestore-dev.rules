rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Regras temporárias para migração - APENAS PARA DESENVOLVIMENTO/EMULATOR
    match /{document=**} {
      allow read, write: if true;
    }
  }
}